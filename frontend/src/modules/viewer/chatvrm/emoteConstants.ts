/**
 * Ported from pixiv/ChatVRM.
 * Original: src/features/emoteController/emoteConstants.ts
 */

/** Duration eyes stay closed during a blink (seconds). */
export const BLINK_CLOSE_MAX = 0.12;

/** Duration eyes stay open between blinks (seconds). */
export const BLINK_OPEN_MAX = 5;
