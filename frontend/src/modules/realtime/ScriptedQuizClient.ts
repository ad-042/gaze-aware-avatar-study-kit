/**
 * Placeholder for a scripted (offline) conversation mode.
 *
 * When realtime is disabled the conversation step currently shows a static
 * "Voice chat not available" message. A full ScriptedQuizClient that walks
 * the participant through pre-authored questions would be a future extension
 * beyond v1 scope.
 *
 * This file exists as a placeholder for the scripted quiz path. It intentionally
 * exports nothing — the fallback path is handled inline in
 * StudyFlow.renderVoiceBar().
 */
