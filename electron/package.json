{
  "name": "gaze-aware-avatar-study-kit-electron",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && electron . --dev",
    "start": "tsc && electron .",
    "lab": "tsc && electron . --kiosk",
    "build:release": "tsc && electron-builder --dir",
    "build:installer": "tsc && electron-builder"
  },
  "devDependencies": {
    "electron": "^32.3.3",
    "electron-builder": "^25.1.8",
    "typescript": "^5.5.0"
  }
}
